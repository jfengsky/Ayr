import React from 'react';
import Header from './Header'
import PageType from './PageType'

const Root = () => (
    <div>
        <Header type={'edit'} />
        <PageType />
    </div>
)

export default Root